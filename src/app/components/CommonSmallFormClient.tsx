"use client";
import CommonSmallForm from "./CommonSmallForm";

export default function CommonSmallFormClient({ formType }: { formType: string }) {
  return <CommonSmallForm formType={formType} />;
}
